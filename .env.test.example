# MŌVE audit pipeline credentials
# Copy to .env.test (gitignored) en vul in.
# Gebruikt door: audits/playwright/walk-through.spec.ts (ux-auditor agent)

TEST_USER_EMAIL=
TEST_USER_PASSWORD=

# Optional — override base url voor staging audit
# BASE_URL=https://staging.move-app.be
